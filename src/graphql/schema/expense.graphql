

type Expense {
  _id: ID!
  amount: Float!
  tags: [Tag!]!
  geo: Geo!
  date: Date!
}

input ExpenseInfo {
  amount: Float!
  geo: GeoInput!
  tags: [ID!]!
  date: Date!
}

type Query {
  getMyExpenses: [Expense!]!
}

type Mutation {
  create_expense(data: ExpenseInfo!): Status!
}

